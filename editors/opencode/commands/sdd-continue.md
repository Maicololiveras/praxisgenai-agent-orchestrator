---
description: Continue the next dependency-ready SDD phase
agent: sdd-orchestrator
---

Continue the active SDD change. If `{args}` is provided, use it as the target change name. Recover state first if needed, then run only the next dependency-ready phase.
